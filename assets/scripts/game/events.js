'use strict';

let playerOnesTurn = true;

const addHandlers = () => {
  $('.box').on('click', function(event){
    if(true){
    }else{
      $('h3').show('slow');
    }
  });
}

module.exports = {
  addHandlers,
};
